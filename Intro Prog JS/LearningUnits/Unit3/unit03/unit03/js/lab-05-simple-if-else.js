function lab05() {
    "use strict";

    // Declarations
    let firstString;
    let secondString;
    let outputMessage;

    // Variable Assignment
    firstString = prompt("Enter a string:");
    secondString = prompt("Enter another:");

    
}